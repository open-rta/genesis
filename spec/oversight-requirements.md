# Oversight Requirements

A runtime MUST provide a way to inspect execution state during operation or inspect result history after operation.

Minimum expectations:
- oversight record references the objective
- observation time is recorded
- state snapshot or status summary is available

Open RTA does not prescribe whether oversight is delivered through APIs, files, or operator interfaces.
